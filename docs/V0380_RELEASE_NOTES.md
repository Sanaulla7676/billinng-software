# v0.38.0

## SAAN Billing Software

This release is intended to publish the current `main` branch as the latest Windows build.

### Release artifacts

- Windows x64 NSIS installer
- Windows x64 portable executable

### Verification

The GitHub Actions workflow must successfully build the current source commit and verify the generated `.exe`, `.blockmap`, and update metadata before this release is treated as production-ready.
