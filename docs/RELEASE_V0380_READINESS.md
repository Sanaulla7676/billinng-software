# v0.38.0 Release Readiness

Target release: v0.38.0

Source of truth: `main` branch.

Release requirements:

1. Build the current `main` branch for Windows x64.
2. Verify the resulting NSIS `.exe` and portable `.exe` artifacts exist.
3. Publish the artifacts to the GitHub release tagged `v0.38.0`.
4. Mark the release as the latest release.
5. The README direct-download link must continue pointing to `/releases/latest` so users receive the newest published release.
6. Record the source commit SHA in the release notes.
7. Do not describe the release as current until the workflow succeeds and the artifacts are available.

Current application product name: SAAN Billing Software.
